import 'package:flutter/material.dart';

class TypeIncomeView extends StatelessWidget {
  const TypeIncomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tipo de Receita/Despesa')),
      body: const Center(child: Text('Teste')),
    );
  }
}
